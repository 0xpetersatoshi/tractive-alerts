[project]
name = "tractive-alerts"
version = "0.1.0"
description = "Send Tractive tracker battery alerts to Telegram."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiotractive>=1.0.3",
]
